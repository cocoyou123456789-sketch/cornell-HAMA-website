# Cornell Hospitality Asset Manager Association

Static website for Cornell HAMA, modeled after the Cornell Real Estate Club homepage format.

Open `index.html` directly in a browser, or run a local static server:

```sh
python3 -m http.server 8775
```
